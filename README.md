# Toggle Debug Mode

Simple web-extension that toggles one or more query parameters.

Open extension options to configure:
- Parameter name
- Value kind (`string`, `number`, `boolean`)
- Value for each parameter

All configured parameters are toggled together when you click the extension icon.
